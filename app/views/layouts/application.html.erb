<!DOCTYPE html>
<html id="app" class="antialiased">
  <%= render "shared/head" %>

  <body class="min-h-screen pb-12 font-sans font-normal leading-normal text-gray-800 bg-gradient-to-r from-pink-300 via-purple-300 to-indigo-400">
    <%= render "shared/flash_notice" %>
    <%= render "shared/header" %>

    <main class="px-4 lg:px-10">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>
